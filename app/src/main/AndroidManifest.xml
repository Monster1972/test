<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.example.old.yamade">

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">

    <activity android:name="com.example.new_.motionlayout.MotionLayoutActivity"/>
    <activity android:name="com.example.new_.encryption.EncryptionActivity" />
    <activity android:name="com.example.new_.activity.ViewActivity" />
    <activity android:name=".test_opengl.OpenGlEsActivity" />
    <activity android:name="com.example.old.anim.Main2Activity" />
    <activity android:name="com.example.new_.design.DesignActivity"/>
    <activity android:name=".Main3Activity" />

    <activity android:name=".MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <service
      android:name=".aidl.MyService"
      android:process="ty.tool" />
  </application>

  <uses-feature
    android:glEsVersion="0x00020000"
    android:required="true" />

</manifest>